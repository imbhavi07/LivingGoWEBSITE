# Review Note

The desktop file `q-dev-chat-2026-05-23.md` appears to be a raw exported chat log, not a maintained project document.

Why it is risky to treat as source of truth:

- It contains mojibake and HTML-escaped text.
- It includes stale instructions that no longer match the current codebase.
- It mixes decisions, temporary debugging, and superseded advice in one stream.

Recommendation:

- Keep the original desktop file only as a personal archive.
- Do not use it as active project documentation.
- Put durable decisions in repo docs instead of chat exports.
